server:
  port: 0
spring:
  cloud:
    config:
      enabled: true
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ingredient-client-2
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/